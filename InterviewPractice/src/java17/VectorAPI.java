package java17;


//The Vector API is part of an incubator module — not included by default in the standard classpath

//import jdk.incubator.vector.FloatVector;
//import jdk.incubator.vector.VectorSpecies;

/*
public class VectorAPI {
    public static void main(String[] args) {
    	float[] arr = {1.1f, 2.2f, 3.3f, 4.4f};
    	var vector = FloatVector.fromArray(FloatVector.SPECIES_PREFERRED, arr);
    	var result = vector.mul(2.0f);
    }
}

*/
