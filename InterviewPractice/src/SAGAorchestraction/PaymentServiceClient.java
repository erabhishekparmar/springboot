package SAGAorchestraction;

/* 
@Service
public class PaymentServiceClient {
    private final RestTemplate restTemplate = new RestTemplate();

    public void processPayment(Long orderId, Double amount) {
        restTemplate.postForObject("http://PAYMENT-SERVICE/payments", new PaymentRequest(orderId, amount), Void.class);
    }

    public void refundPayment(Long orderId) {
        restTemplate.postForObject("http://PAYMENT-SERVICE/refund/" + orderId, null, Void.class);
    }
}
*/